query getRepositoryObject($owner: String!, $name: String!, $expression: String!)
@api(name: github) {
  repository(owner: $owner, name: $name) {
    content: object(expression: $expression) {
      ... on Blob {
        text
      }
    }
  }
}
